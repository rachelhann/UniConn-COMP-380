package com.uniconn.backend.services;

import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Service;
import org.springframework.web.bind.annotation.PathVariable;

import com.uniconn.backend.repositories.*;
import jakarta.transaction.Transactional;

@Service
public class PasswordResetService {
	/* private final UserRepository userRepository;
	   private final PasswordResetRepository passwordResetRepository;
	
	@Transactional
	public String acceptSecretAnswer(Integer userId) {
		// registration -> set reset question and answer
	}
	
	@Transactional
	public String resetPassword(Integer userId) {
		// check answer -> get new password -> update password in users table
	}*/
	
	/* in UserController class
	 	
	 	@PostMapping("/reset-password")
	 	public ResponseEntity<?> passwordReset(@PathVariable Integer userId)   	
	 		 
	*/

}
